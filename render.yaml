# Render build configuration - updated Apr 17, 2026
services:
  - type: web
    name: bva-dashboard
    runtime: node
    buildCommand: npm ci && npm install -w client && npm run build -w client
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 20
