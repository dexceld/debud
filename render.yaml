services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm install && npm run build -w client
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 20
