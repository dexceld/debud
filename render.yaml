services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm ci && npm run build -w shared && npm run build -w client
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 18
