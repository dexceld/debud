services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm install && cd client && npm install && npx tsc --noEmit && npx vite build
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 18
