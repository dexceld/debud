services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm install && cd client && npm run build
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 18
