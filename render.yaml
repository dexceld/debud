services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm ci && cd client && npm install && npx vite build
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 20
