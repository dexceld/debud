services:
  - type: static
    name: bva-dashboard
    runtime: static
    buildCommand: npm ci && cd client && npx vite build
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_VERSION
        value: 20
